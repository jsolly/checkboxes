# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
npm-debug.log*

# environment variables
.env
.env.local
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# vercel (local CLI state; the hook passes VERCEL_ORG_ID/VERCEL_PROJECT_ID)
.vercel
# editor (local symlinks to ~/.agents rules — machine-specific)

# Superpowers brainstorm scratch (local server state, not project artifacts)
.superpowers/

# Cursor: track local-editor rules; ignore other editor state
.cursor/*
!.cursor/hooks.json
!.cursor/worktrees.json
!.cursor/rules/
!.cursor/rules/**
!.cursor/CLOUD.md
!.cursor/environment.json
!.cursor/install-cloud-skills.sh


# Materialized agent config (fetched by .dotagents/bootstrap.sh; not committed)
