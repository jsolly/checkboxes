# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
npm-debug.log*

# environment variables
.env
.env.local
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# vercel: track the project link (no secrets), ignore everything else local
.vercel/*
!.vercel/project.json
# editor (local symlinks to ~/.agents rules — machine-specific)

# Superpowers brainstorm scratch (local server state, not project artifacts)
.superpowers/

# Cursor: track cloud config and rules; ignore other local editor state
.cursor/*
!.cursor/environment.json
!.cursor/hooks.json
!.cursor/rules/
!.cursor/rules/**


# Materialized agent config (fetched by .dotagents/bootstrap.sh; not committed)
